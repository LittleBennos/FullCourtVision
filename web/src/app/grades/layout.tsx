export const metadata = {
  title: "Grades",
  description: "Browse basketball grades and divisions across Victorian competitions. Filter by season, competition, and organisation.",
};

export default function GradesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
